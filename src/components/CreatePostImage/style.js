import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  postImageContainer: {
    alignSelf: 'center',
    padding: 10,
  },
  cancelStyle: {
    position: 'absolute',
    right: 0,
    zIndex: 1,
    margin: 10,
  },
});
export default style;
